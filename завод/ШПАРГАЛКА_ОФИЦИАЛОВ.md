# ШПАРГАЛКА ОФИЦИАЛОВ — контекст роли зрячего этажа

Заведена 17.08.2026 под зрячий этаж `проверка_промпта.py` (`--зрячий`), вторую половину
претензии оператора П14: «а официал он неизменяем, он ориентир».

**Что это.** Выжимка разделов **1.10, 1.11, 1.12** файла
`/root/АВТОЗАВОД/ОФИЦИАЛЫ_NANO_BANANA_PRO.md` — полный текст, страницы, статус проверки и
адреса живут ТОЛЬКО там. Здесь — сжатое изложение, чтобы роль зрячего этажа держала официал
в контексте, не читая весь файл целиком на каждый вызов.

**Чем это НЕ является.** Это не второй дом смыслов и не второй свод правил. Ни одна строка
ниже не формулирует НОВОЕ правило — только пересказ того, что уже сказано в официале, плюс
номер пункта для ссылки. Если `ОФИЦИАЛЫ_NANO_BANANA_PRO.md` меняется — правится СНАЧАЛА он,
и уже вслед за ним, тем же заходом, эта шпаргалка. Расхождение между файлами — это всегда
ошибка шпаргалки, а не повод трактовать её как самостоятельный источник.

Роль зрячего этажа обязана в каждой находке называть НОМЕР ПУНКТА снизу (1.10 / 1.11 / 1.12).
Находка без номера пункта — не находка, и прибор (`проверка_промпта.py`, функция
`зрячий_разбор`) её отбрасывает сам, а не полагается на дисциплину роли.

---

## 1.10 — Best practices for prompting

Источник: `ОФИЦИАЛЫ_NANO_BANANA_PRO.md`, пункт **1.10**
(docs.cloud.google.com/vertex-ai/generative-ai/docs/multimodal/gemini-image-generation-best-practices).

- **Be specific** — конкретика вместо общих эпитетов. Пример Google: не «fantasy armor», а
  «ornate elven plate armor, etched with silver leaf patterns, high collar, pauldrons shaped
  like falcon wings».
- **Provide context and intent** — сказать, ЗАЧЕМ нужен кадр. Пример Google: «create a logo
  for a high-end, minimalist skincare brand».
- **Iterate and refine** — не ждать идеала с первого раза, доводить фоллоу-апом. Пример
  Google: «make the lighting warmer».
- **Use step-by-step instructions** — сложную сцену собирать по шагам: сначала фон, потом
  объект переднего плана, потом ключевой предмет.
- **Describe what you want, not what you don't** — вместо «no cars» → «an empty, deserted
  street with no signs of traffic».
- **Control the camera** — фото/кино-термины: «wide-angle shot», «macro shot», «low-angle
  perspective».
- **Prompt for images** — явно писать «create an image of», иначе модель может ответить
  текстом.
- **Pass Thought Signatures** — при мультитёрн-создании/редактировании возвращать thought
  signatures, чтобы сохранить контекст рассуждения.

## 1.11 — Известные ограничения (limitations)

Источник: `ОФИЦИАЛЫ_NANO_BANANA_PRO.md`, пункт **1.11**
(docs.cloud.google.com/vertex-ai/generative-ai/docs/multimodal/gemini-image-generation-limitations).

- Языки наилучшего качества для Gemini 3 Pro Image: ar-EG, de-DE, EN, es-MX, fr-FR, hi-IN,
  id-ID, it-IT, ja-JP, ko-KR, pt-BR, **ru-RU**, ua-UA, vi-VN, zh-CN.
- Максимум **14 изображений** на входе для лучшего результата.
- Дословно: «When generating an image containing text, first generate the text and then
  generate an image with that text» — сначала выработать текст, потом просить кадр с ним.
- Модель может выдать не то число картинок, что просили; может ответить ТОЛЬКО текстом при
  двусмысленном промпте; может нарисовать текст картинкой вместо ожидаемого результата.
- Аудио и видео на вход генерация не принимает.
- Небезопасный промпт → ответ с `FinishReason = STOP`.

## 1.12 — Ultimate prompting guide (официальный блог Google Cloud)

Источник: `ОФИЦИАЛЫ_NANO_BANANA_PRO.md`, пункт **1.12**
(cloud.google.com/blog/products/ai-machine-learning/ultimate-prompting-guide-for-nano-banana).

- Формула T2I: `[Subject] + [Action] + [Location/context] + [Composition] + [Style]`.
- Формула мульти-рефа: `[Reference images] + [Relationship instruction] + [New scenario]` —
  явно объяснить модели, ЧЕМ является каждый реф («вот этот набросок — структура, вот эта
  ткань — текстура»).
- Редактирование: семантическая маска словами (inpainting текстом) + «be explicit about what
  to keep exactly the same».
- Текст на кадре, 4 правила дословно: (1) слова заказанного текста — в **кавычках**;
  (2) **назвать шрифт** словами («bold, white, sans-serif», «Century Gothic»); (3) писать на
  ОДНОМ языке и указать язык вывода (локализация); (4) «text-first hack» — сначала выработать
  текст диалогом, затем просить кадр с готовым текстом.
- Свет называется рецептом-формулировкой: «three-point softbox setup», «chiaroscuro lighting
  with harsh, high contrast», «golden hour backlighting creating long shadows».
- Камера и оптика называются словами: конкретный аппарат (GoPro / Fujifilm / одноразовая
  мыльница) и объектив («low-angle shot with shallow depth of field (f/1.8)», «wide-angle»,
  «macro»).
- Цвет и плёнка называются словами: «as if on 1980s color film, slightly grainy»,
  «cinematic color grading with muted teal tones».
- Материальность называется явно, а не общим словом: не «пиджак», а «navy blue tweed»; для
  мокапа называть поверхность («minimalist ceramic coffee mug»).
- Промпт рекомендуется начинать сильным глаголом, задающим основную операцию.
- Чего Google прямо называет НЕ делать: отрицательные формулировки, голые списки ключевых
  слов, забытая типографика, общие эпитеты без материала/света/камеры.

---

Полный текст, статус проверки каждой страницы и все адреса — только в
`/root/АВТОЗАВОД/ОФИЦИАЛЫ_NANO_BANANA_PRO.md`. Эта шпаргалка своих правил не вводит: если
Google о чём-то не говорит, здесь об этом тоже молчание, а не восполнение своей формулировкой.
