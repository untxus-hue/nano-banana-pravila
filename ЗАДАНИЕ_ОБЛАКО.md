# Задание для облачной сессии: сшивка тем (шаг 2)

Ты прораб. Работаешь в корне этого репозитория. Все пути вида /root/БАЗА_ПРАВИЛ/исходники_кандидаты/сырьё/google/… в файлах ТЗ читать как пути от корня этого репозитория (тот же хвост: сведение/…, извлечение/страницы/…). Папок АВТОЗАВОД, СИСТЕМА_ЗАВОДА, data, rule_engine здесь нет, запреты на них просто не нужны. В интернет не ходить.

Что делать:
1. Прочитать сведение/ТЗ_СШИВКА.md и сведение/ТЗ_СВЕДЕНИЕ.md целиком.
2. Темы для сшивки (16): api_код (части ч1, ч2, ч3), промптинг (ч1, ч2), рефы (ч1, ч2), редактирование (ч1, ч2), лимиты_квоты (ч1, ч2), модели_сравнение (ч1, ч2), безопасность_водяной_знак (ч1, ч2), продукт_ui (ч1, ч2), и по одной части: стиль, текст_в_кадре, форматы_разрешение, цены, камера_свет_композиция, мультиход, grounding, thinking. Файл сведение/темы/другое.json — не отдельная тема для сшивки: его ядра раздаются по темам по полю тема_предлагаемая; ядра с тема_предлагаемая = «другое» (13 штук) собрать в отдельный файл сведение/сшито/другое.json по той же канонической форме.
3. На каждую тему запускать отдельного субагента (модель Opus, не Sonnet), не больше 6 одновременно. Промпт субагенту: прочитать ТЗ_СШИВКА.md и выполнить его для темы <тема>, вход и выход по ТЗ, скрипты только в своей подпапке во временной папке, в конце самопроверка скриптом, отчёт кратко по-русски. Субагенты ничего не выдумывают: непонятно — вопрос в отчёт.
4. После каждой партии: проверить своим скриптом каждый готовый файл сведение/сшито/<тема>.json (валидный JSON, коды уникальны, карта_кодов покрывает все старые коды, E-коды входа не потеряны, нет кириллицы в полях _en кроме цитат, перечисления из ТЗ). Ошибки вернуть тому же субагенту на исправление. Потом git add сведение/сшито и git commit с сообщением «сшивка: <темы>».
5. В конце написать сведение/сшито/ОТЧЁТ_СШИВКИ.md: по каждой теме ядер на входе / на выходе, слито, спор, вопросов снято / осталось; общий итог; все сомнения субагентов. Закоммитить.

Правила: ничего не удалять из сведение/темы/; результаты только в сведение/сшито/; каждый файл сохранять сразу, как готов. Если субагент не справился (обрыв, переполнение) — перезапустить его на ту же тему с указанием, что уже есть черновик.
